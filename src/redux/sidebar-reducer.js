let initialState = {
    friends: [
        {id: 1, name: 'John'},
        {id: 2, name: 'Olivia'},
        {id: 3, name: 'Noah'},
        {id: 4, name: 'Charlotte'},
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer